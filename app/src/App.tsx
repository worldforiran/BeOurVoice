import styles from './App.module.css';
import Hashtags from './Hashtags';

interface IProps {};

function App({}: IProps) {
  return (
    <div>
      <Hashtags />
    </div>
  );
}

export default App;
